import '.';
import {
    Meta,
    Story,
    Preview,
} from '@gig/magic-wand-spellbook/dist/storybook/helpers';
import ReadMe from './README.md';

<Meta title="UI Kit|Search Field" />

<ReadMe.parameters.docs.page />

## Translations

| Unique ID                 | Purpose                           |
| ------------------------- | --------------------------------- |
| search.search.placeholder | Placeholder text for search field |

## Stories

Search field displays a field with search button

<Preview>
    <Story name="Default">
        <magic-ui-search-field />
    </Story>
</Preview>

```html
<magic-ui-search-field />
```

### With value

If search field has value set it shows button to clear search query which emits
`clear` event when clicked

<Preview>
    <Story name="With Value">
        <magic-ui-search-field value="search query" />
    </Story>
</Preview>

```html
<magic-ui-search-field value="search query" />
```
